# 🚀 دليل نشر موقع طبعة على Netlify

## هل الموقع متوافق مع Netlify؟

**نعم، الموقع متوافق تماماً مع Netlify!** ✅

الموقع مبني كـ **موقع ثابت (Static Website)** وهذا يعني أنه مثالي للنشر على Netlify. لا يحتاج إلى خادم خلفي (Backend) أو قاعدة بيانات.

---

## الخطوات الأساسية للنشر على Netlify

### الخطوة 1️⃣: إعداد مستودع GitHub

أولاً، تحتاج إلى رفع الكود على GitHub:

```bash
# 1. إنشاء مستودع جديد على GitHub
# ثم قم بنسخ رابط المستودع

# 2. في مجلد المشروع، قم بتهيئة Git
git init
git add .
git commit -m "Initial commit: Tab3ah store"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/tab3ah_store.git
git push -u origin main
```

### الخطوة 2️⃣: ربط Netlify بـ GitHub

1. **اذهب إلى** [netlify.com](https://netlify.com)
2. **اضغط على** "Sign up" أو "Log in"
3. **اختر** "GitHub" للدخول
4. **اسمح** لـ Netlify بالوصول إلى حسابك على GitHub

### الخطوة 3️⃣: إنشاء موقع جديد

1. **اضغط على** "New site from Git" أو "Add new site"
2. **اختر** "GitHub" كـ Git provider
3. **ابحث عن** مستودع `tab3ah_store`
4. **اختر الفرع** `main`

### الخطوة 4️⃣: إعدادات البناء (Build Settings)

عندما تصل إلى صفحة إعدادات البناء، أدخل:

| الحقل | القيمة |
|------|--------|
| **Build command** | `pnpm build` |
| **Publish directory** | `dist` |

**الصورة:**
```
Build command:      pnpm build
Publish directory:  dist
```

### الخطوة 5️⃣: النشر

1. **اضغط على** "Deploy site"
2. **انتظر** حتى ينتهي البناء (عادة 2-3 دقائق)
3. **ستحصل على رابط موقعك** مثل: `https://your-site-name.netlify.app`

---

## إعدادات إضافية مهمة

### تفعيل Netlify CMS (لوحة التحكم)

لتفعيل لوحة التحكم لإدارة المنتجات:

1. **اذهب إلى** إعدادات الموقع في Netlify
2. **اختر** "Access control" → "OAuth"
3. **اضغط على** "Install provider"
4. **اختر** GitHub
5. **أكمل المصادقة**

الآن يمكنك الدخول إلى لوحة التحكم على: `https://your-site.netlify.app/admin`

### إعادة التوجيه (Redirects)

لضمان أن جميع المسارات تعمل بشكل صحيح، أضف ملف `netlify.toml`:

```toml
[build]
  command = "pnpm build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

هذا الملف موجود بالفعل في المشروع إن شاء الله.

---

## خطوات متقدمة

### ربط نطاق مخصص (Custom Domain)

1. **اشتر نطاق** من أي موفر (مثل Namecheap, GoDaddy)
2. **في Netlify، اذهب إلى** "Domain settings"
3. **اضغط على** "Add custom domain"
4. **أدخل اسم النطاق** (مثل: tab3ah.com)
5. **اتبع التعليمات** لتحديث DNS

### تفعيل HTTPS

**Netlify توفر HTTPS مجاناً تلقائياً!** ✅

لا تحتاج إلى فعل شيء، سيتم تفعيل شهادة SSL تلقائياً.

### متغيرات البيئة (Environment Variables)

إذا كنت تستخدم Snipcart:

1. **اذهب إلى** "Site settings" → "Build & deploy" → "Environment"
2. **أضف متغيرات جديدة:**
   - `VITE_SNIPCART_KEY`: مفتاح Snipcart الخاص بك

---

## استكشاف الأخطاء

### المشكلة: الموقع يظهر خطأ 404

**الحل:** تأكد من أن `publish directory` هو `dist`

### المشكلة: الصور لا تظهر

**الحل:** تأكد من أن مسارات الصور صحيحة (يجب أن تبدأ بـ `/`)

### المشكلة: لوحة التحكم لا تعمل

**الحل:** تأكد من تفعيل GitHub OAuth في Netlify

---

## نصائح مهمة

✅ **قبل النشر:**
- اختبر الموقع محلياً: `pnpm dev`
- تأكد من أن جميع الملفات مرفوعة على GitHub
- تحقق من أن `package.json` يحتوي على جميع المتطلبات

✅ **بعد النشر:**
- اختبر الموقع على الجوال
- جرب البحث والتصفية
- اختبر إضافة المنتجات إلى السلة
- تأكد من أن اللغة العربية تظهر بشكل صحيح

---

## الخطوات السريعة (ملخص)

```bash
# 1. إرسال الكود إلى GitHub
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/tab3ah_store.git
git push -u origin main

# 2. في Netlify:
# - اضغط "New site from Git"
# - اختر GitHub و المستودع
# - Build: pnpm build
# - Publish: dist
# - اضغط Deploy

# 3. انتظر النشر (2-3 دقائق)
# 4. احصل على رابطك!
```

---

## الدعم والمساعدة

إذا واجهت أي مشاكل:

1. **تحقق من سجلات البناء** في Netlify (Build logs)
2. **تأكد من أن Node.js و pnpm مثبتة** محلياً
3. **جرب حذف `node_modules` وتثبيتها مرة أخرى**

```bash
rm -rf node_modules
pnpm install
```

---

**تهانينا! موقعك الآن على الإنترنت! 🎉**

للمزيد من المعلومات، زر: [Netlify Docs](https://docs.netlify.com)
